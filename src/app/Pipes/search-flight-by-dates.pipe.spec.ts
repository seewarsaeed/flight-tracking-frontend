import { SearchFlightByDatesPipe } from './search-flight-by-dates.pipe';

describe('SearchFlightByDatesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFlightByDatesPipe();
    expect(pipe).toBeTruthy();
  });
});
