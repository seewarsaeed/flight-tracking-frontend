import { SearchFlightByAirportPipe } from './search-flight-by-airport.pipe';

describe('SearchFlightByAirportPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFlightByAirportPipe();
    expect(pipe).toBeTruthy();
  });
});
